- 2026-05-04: Researched Flue (`withastro/flue`) from the provided `flueframework.com` link, cloned upstream into `.refs/withastro__flue`, harvested transferable session/compaction/delegation/connector primitives into `.docs/research/2026-05-04-flueframework-harvest.md`, and recorded a VAR1-specific adopt/adapt/reject implementation matrix with ordered next-slice recommendations.
- 2026-05-04: Refreshed public-facing validation language before publishing the hardening/research checkpoint. Current local gates passed with `.\scripts\zigw.ps1 build test --summary all` -> `90/90 tests passed`, `.\scripts\health.ps1` -> `status: ready`, `.\zig-out\bin\VAR1.exe health --json` -> `ok: true`, and `.\zig-out\bin\VAR1.exe tools --json` -> strict tool schemas plus `iex` availability metadata.
- 2026-05-04: Harvested the Turing Post nine-method MAS survey with Insect plus primary arXiv pages, then mapped RecursiveMAS, OMC, OrgAgent, CORAL, LLMA-Mem, Agentic FL, CASCADE, GRASP, and Reinforced Agent into VANTARI-safe kernel adoption boundaries. Artifact: `.docs/research/2026-05-04-multi-agent-systems-9-methods.md`. Primary recommendation: pre-tool review gate, scoped escalation contracts, transcript-safe memory topology, capability profiles, and evaluator/heartbeat separation without parallel runtimes.
- 2026-05-04: Created planning-spec chain `024-agent-capability-governance` to turn the MAS research into a VAR1-native implementation sequence: pre-tool review primitives, executor loop review integration, scoped delegation, typed capability profiles, transcript-safe derivative memory, evaluator/heartbeat event boundaries, docs, and terminal verification. The chain explicitly depends on the live `021` auth chain before overlapping `shared/types.zig` work and encodes the user requirement to improve architecture/capability without copied MAS architecture or runtime regressions.
- 2026-05-04: Implemented the executable `024-agent-capability-governance` kernel slice: `src/core/tools/review.zig` now classifies tool risk before dispatch; `loop.zig` records `tool_requested -> tool_reviewed -> tool_completed/tool_blocked`; `src/core/agents/profile.zig` and `scope.zig` bind child delegation to typed capability profiles; `src/core/memory/derivative.zig` keeps memory sequence-bound instead of transcript-shaped; and `src/core/evaluation/events.zig` appends redacted non-mutating heartbeat/evaluator evidence. Documentation and the MAS research artifact were updated to the shipped contract. Validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `95/95 tests passed`.
- 2026-05-05T04:19:24+02:00 - Closed 025 tool-risk metadata ownership: moved review risk truth into module-owned `ToolDefinition.review_risk`, passed the active catalog into pre-dispatch review, exposed `review_risk` in catalog JSON/text, blocked context-unavailable tools before dispatch, and validated with `.\\scripts\\zigw.ps1 build test --summary all` (`95/95 tests passed`) plus invariant scans for removed private risk name arrays.
- 2026-05-05: Cloned Scion into `.refs/savageops__scion` at `2fad47cf1248855b2dc1073a20972dd0bfd4ef2a`, reviewed its container/grove/hub/broker/tmux architecture against VAR1, and stored the lightweight transfer matrix at `.docs/research/2026-05-05-scion-harvest.md`. Adopt/adapt candidates are limited to a phase/activity projection, sticky diagnostic activities, typed session-event normalization, and readiness strata; Scion's distributed platform, container runtime, tmux dependency, and Hub event fanout were explicitly rejected for current VAR1.
- 2026-05-05: Corrected the Scion harvest decision semantics after review: `reject` now means not core-owned/default-runtime material, while containerized agents, templates, transfer, and terminal attach remain possible future optional modules behind typed sockets. Updated the global Codex AGENTS doctrine to keep health/readiness/diagnostics minimal and scalable while prioritizing proven capability and customer-facing leverage.
- 2026-05-06: Ran a product-language and attention pass over the VAR1 browser client plus public README surfaces. The client now presents as an agent session workbench, uses task-first action labels and calmer empty/error states, shifts away from editorial/beige demo styling into a compact enterprise workbench layout, and records the UX/content rationale in `.docs/research/2026-05-06-product-language-and-attention-pass.md`.
- 2026-05-06: Added a 300-case VAR1 pipeline matrix covering context overflow classification, context budget heuristics, tool socket validation, plugin manifest validation, tool review/runtime hints, bridge access policy, session-id extraction, and redaction behavior. Stored the coverage sweep at `.docs/research/2026-05-06-pipeline-test-coverage-sweep.md`; the canonical backend gate now validates at `.\scripts\zigw.ps1 build test --summary all` -> `395/395 tests passed`.
- 2026-05-06: Completed the Session Integrity and Recovery Gate hardening pass for `.var/sessions/<session-id>/`: JSONL appends now isolate partial trailing rows with a single-handle tail-byte check before adding new durable records, store tests cover corrupted and missing session ledgers, context-builder tests prove poisoned checkpoint suffixes do not override the latest valid checkpoint plus raw transcript suffix, compactor tests assert immutable `messages.jsonl` versus checkpoint-only `context.jsonl`, and executor tests now replay provider-overflow, failure, and cancellation lifecycle events from `events.jsonl`. Validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `400/400 tests passed`.
- 2026-05-06: Ran the codebase-wide low-value architecture pass across the live VAR1 backend lane. Fixed explicit prompt-layer configuration so missing or empty configured files fail closed instead of silently degrading to built-in defaults; removed O(n) docs-sync preflight reads that only proved file existence; and removed active-contract migration vocabulary by renaming model-visible file-effect `LEGACY_OUTPUT` to `DISPLAY_OUTPUT`. Validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `401/401 tests passed`.
- 2026-05-06: Ran a third backend architecture hygiene pass focused on easy high-leverage primitive defects. Bounded `src/shared/fsutil.zig` text reads at `64 MiB` through `fsutil.max_text_file_bytes`, removing the hidden `maxInt(usize)` allocation contract from shared file ingestion without adding a parallel reader or caller-local limits. Validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `401/401 tests passed`.
- 2026-05-06: Ran a fourth backend architecture hygiene pass over shared filesystem mutation primitives. Replaced truncate-before-write `fsutil.writeText` with Zig's same-directory `AtomicFile` commit path, so `session.json`, docs sync files, auth state, and other canonical text records no longer inherit a partial-write corruption window from the shared helper. Added focused replacement coverage and validated with `.\scripts\zigw.ps1 build test --summary all` -> `402/402 tests passed`.
- 2026-05-06: Collapsed the live backend lane into `apps/backend` with no compatibility directory, alias, or second runtime root. Moved the Zig package, scripts, `.var` runtime state, `.toolchain`, docs, tests, playground, and build artifacts into the canonical lane; updated AGENTS/root/backend/client docs, pending execution specs, ignore rules, and live fixtures to reference `apps/backend`; and validated the flattened backend from the new root.
- 2026-05-06: Ran another low-value architecture pass after the backend-lane collapse. Moved canonical file-tool definitions into `src/core/tools/registry.zig` so runtime catalog output consumes the registry owner instead of preserving a second file-tool definition array, added registry parity assertions, removed inert TODO/placeholder wording from live backend and public configuration docs, and revalidated from `apps/backend` with `.\scripts\zigw.ps1 build test --summary all` -> `402/402 tests passed`, `.\scripts\health.ps1` -> `status: ready`, and `VAR1.exe tools --json` showing `search_files` available through `iex`.
- 2026-05-06: Ran a Ventari cleanup pass after the backend-lane collapse. Removed ignored pre-collapse and generic runtime-research artifacts, deleted local cache/build/playground/audit/database detritus, made deprecated runtime roots visible by removing their ignore rule, added `/data/` as local database state, and updated live docs/scripts/tests away from stale pre-product naming. Verification scan showed no tracked deprecated lane, deprecated runtime-root, old project, or unrelated orchestration references remain.
- 2026-05-06: Ran a blue-chip architecture pass over remaining low-value seams. Replaced the Bash Gemma bridge smoke's removed HTTP task-route probe with the canonical token-gated JSON-RPC `/rpc` session/create -> session/send -> session/get path, hardened the probe with fail-fast HTTP calls, generated JSON params, and explicit bridge-token checks, corrected prompt-envelope documentation to state configured prompt layers fail closed, removed stale runtime-harness wording from the compiled internal guardrail copy, and persisted the repo-level depth standard in `AGENTS.md`.
- 2026-05-07: Closed the first institutional-grade runtime hardening slice from the GLM 5.1 health audit. Auth ledger reads now tolerate UTF-8 BOM at the `core/auth/store.zig` boundary with regression coverage, and CLI JSON-RPC remote failures now exit through a stable `VAR1_ERROR` envelope instead of emitting a Zig stack trace after known kernel errors. Validation passed with `.\\scripts\\zigw.ps1 build test --summary all` -> `403/403 tests passed`, installed binary rebuild, BOM-prefixed auth health proof, restored auth health proof, and missing-session CLI error-envelope proof.
- 2026-05-07: Closed the next reliability-review slice over the live backend harness. Extracted the CLI JSON-RPC failure envelope into a pure tested formatter, moved adversarial envelope coverage into the aggregate CLI test lane, and replaced generic provider `InvalidHttpResponse` collapse with explicit malformed-header, short-body, and malformed-chunk transport errors proven through native HTTP tests. Validation passed with `.\\scripts\\zigw.ps1 build test --summary all` -> `409/409 tests passed`.
- 2026-05-07: Closed a user-visible pipeline recovery gap in the backend host surface. `session/get` now reconciles stale `running` sessions into a durable `failed` session state when no in-memory kernel execution owns them, appending a `session_failed` event plus persisted failure reason so clients do not keep rendering an orphaned live run as active. Validation passed from `apps/backend` with `.\\scripts\\zigw.ps1 build test --summary all` -> `409/409 tests passed`, `.\\scripts\\zigw.ps1 build --summary all`, `git diff --check`, and `VAR1.exe health --json` -> `ok: true`.
- 2026-05-07: Aligned the host lifecycle surfaces so state, durable session events, and bridge notifications move through one contract. Host-owned cancellation now records durable `session_cancelled` evidence before-run; stale running sessions reconcile through both `session/get` and `session/list`; cancelling a stale running session closes it as `cancelled` instead of returning a hollow cancellation miss; executor failure notifications now emit `failed` status after the persisted failure transition; and tests assert dashboard/list, detail, cancellation, and failure-notification paths. Validation passed from `apps/backend` with `.\\scripts\\zigw.ps1 build test --summary all` -> `409/409 tests passed`, `.\\scripts\\zigw.ps1 build --summary all`, `git diff --check`, and `VAR1.exe health --json` -> `ok: true`.
- 2026-05-07: Closed the final-response telemetry ordering gap found by deeper lifecycle tests. The executor now persists assistant output and marks the session `completed` before appending/emitting the `assistant_response` event, so chat/bridge consumers do not receive the final response with stale `running` status. The success-path runtime test now captures hook-visible event metadata and asserts `assistant_response` carries `completed`. Validation passed from `apps/backend` with `.\\scripts\\zigw.ps1 build test --summary all` -> `409/409 tests passed`, `.\\scripts\\zigw.ps1 build --summary all`, `git diff --check`, and `VAR1.exe health --json` -> `ok: true`.
- 2026-05-07: Closed another stale-session ownership gap surfaced by challenging host RPC tests. `session/resume` and `session/compact` now share the stale running-session reconciliation contract already used by `session/get`, `session/list`, and stale `session/cancel`, preventing resume views or manual compaction from treating orphaned `running` sessions as live-owned. Added host tests for stale resume and stale compaction plus a shared stale-event fixture. Validation passed from `apps/backend` with `.\\scripts\\zigw.ps1 build test --summary all` -> `409/409 tests passed`, `.\\scripts\\zigw.ps1 build --summary all`, `git diff --check`, and `VAR1.exe health --json` -> `ok: true`.
- 2026-05-07: Closed the `session/send` stale/failed implicit-execution gap. Send now reconciles stale running ownership before dispatch and treats `failed` as terminal unless the caller provides a new prompt, preventing a no-prompt send from accidentally retrying an orphaned or failed session. Added host RPC tests that prove stale no-prompt send returns the reconciled failure and failed no-prompt send returns the existing failure reason without touching the provider transport. Validation passed from `apps/backend` with `.\\scripts\\zigw.ps1 build test --summary all` -> `409/409 tests passed`, `.\\scripts\\zigw.ps1 build --summary all`, `git diff --check`, and `VAR1.exe health --json` -> `ok: true`.
- 2026-05-07: Ran the Pi/Codex runtime reference harvest against `.refs/badlogic__pi-mono` and `.refs/openai__codex`, preserving only smaller VAR1-native invariants: one context-builder boundary, source-order transcript persistence, append-only event evidence, and bounded future transport behavior. Stored the adopt/adapt/reject matrix in `.docs/research/2026-05-07-pi-codex-runtime-harvest.md` and added a multi-tool pipeline test proving assistant source-order tool persistence before follow-up provider context and final response events. Validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `410/410 tests passed`, `.\scripts\zigw.ps1 build --summary all`, `git diff --check`, and `VAR1.exe health --json` -> `ok: true`.
- 2026-05-07: Deepened the Pi/Codex port from happy-path source-order proof into crash/recovery transcript integrity. `core/context/builder.zig` now rejects unresolved assistant tool-call tails and orphan/misordered tool-result rows before provider dispatch, preserving append-only `messages.jsonl` while preventing malformed model-visible context. `core/context/compactor.zig` now keeps assistant tool-call batches together in the raw suffix when checkpoint planning would otherwise split the assistant request from its tool-result rows. Added context-builder negative tests, a compaction boundary test, and an executor preflight test proving the provider transport is not called and the session fails durably with `UnresolvedToolCallTranscript`. Validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `414/414 tests passed`, `.\scripts\zigw.ps1 build --summary all`, `git diff --check`, and `VAR1.exe health --json` -> `ok: true`.
- 2026-05-07: Closed the provider-overflow tool-context duplication gap surfaced by a deeper executor pipeline test. Completed durable tool batches now advance the executor preserve boundary after `messages.jsonl` writes, so provider-overflow and threshold rebuilds read that context through the canonical builder instead of replaying both persisted and in-memory copies. Added an adversarial tool-overflow retry test that first failed with duplicated `call_retry_boundary` context, then passed after the ownership transfer. Validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `415/415 tests passed`.
- 2026-05-07: Ran live Z.AI `glm-5.1` harness validation through the real `VAR1` executable. Four scenarios covered long iterative artifact creation, file mutation/readback, unsupported `claude -p` command-boundary handling, and parent/child delegation. Results are documented in `.docs/research/2026-05-07-zai-live-harness-validation.md`: 3 parent runs completed, the long 2,000-line stress run failed durably with `ReadFailed` after 800 persisted lines, and the child auditor completed. Auth was restored to the pre-test provider after validation.
- 2026-05-08: Shifted the Z.AI long-run follow-up from reactive resume emphasis to proactive reliability shaping. File mutation tools now publish `maxLength: 8192` schema budgets for generated content fields and enforce the same budget before write/append/replace side effects, with model-visible `ToolPayloadExceeded` guidance for deterministic chunking. Added adversarial tool tests proving oversized write, append, and replace payloads leave files unchanged. Validation passed from `apps/backend` with `.\scripts\zigw.ps1 build test --summary all` -> `416/416 tests passed`.
- 2026-05-08: Re-ran the long Z.AI live harness stress as a 3,000-line artifact generation task using saved `zai` / `glm-5.1`. The canonical session `session-1778206136387-d5f86881fdd1abc` completed with 30 `append_file` calls, 2 `read_file` calls, 0 tool errors, max tool arguments of 6,286 bytes, and an exact 3,000-line artifact at `.var/live-zai-tests/20260508-040854/run01-3000-line-ledger.txt`. Evidence captured in `.docs/research/2026-05-08-zai-3000-line-live-run.md`; auth/config restored to the pre-test provider after completion.
- 2026-05-08: Ran the delegated Z.AI live harness stress where the parent VAR1 session had to launch a child agent to create a 1,000-line artifact. The corrected run `20260508-045833` completed with parent session `session-1778209114297-5faa0822f6ee110d` and child session `session-1778209207161-643c6fadd56c1791`; parent used 1 `launch_agent`, 4 `wait_agent`, 2 `read_file`, and 0 write tools, while the child used 10 `append_file` plus 2 `read_file`, with 0 tool errors and max child argument bytes of 6,396. Artifact `.var/live-zai-tests/20260508-045833/delegated-child-1000-line-ledger.txt` validated at 1,000 lines with 0 format mismatches. Evidence captured in `.docs/research/2026-05-08-zai-delegated-1000-line-live-run.md`; auth/config restored after completion.
- 2026-05-08: Probed active `cli-cloud-llm-v1` with the same delegated 1,000-line harness pattern and a direct one-token smoke. Both failed before assistant/tool execution with durable `BadStatus` sessions (`session-1778210742045-c37e035a06e57f1d`, `session-1778211104604-3e71ecfbc2fda9d1`), empty stdout/stderr artifacts, and no child session/artifact creation, proving provider completion reachability is not established. RunPod vLLM was mapped as a compatible replacement target through `https://api.runpod.ai/v2/<ENDPOINT_ID>/openai/v1`; local environment has `RUNPOD_API_KEY` but no endpoint id/model binding yet.
- 2026-05-08: Provisioned RunPod serverless vLLM control-plane assets for VAR1 testing. Created template `hkyjvfajm3` from `runpod/worker-v1-vllm:v2.18.1` with `Qwen/Qwen2.5-Coder-7B-Instruct`, served model `vantari-qwen2.5-coder-7b`, 8k context, and Hermes tool parser. `ADA_24` endpoint placement stayed throttled, while an `ADA_32_PRO` worker reached readiness but OpenAI direct-route probes timed out and scaled beyond the intended cost envelope; those live probe endpoints were removed. Final retained endpoint `vllm-zfwlutvio6ilfl` is idle with min workers `0`, max workers `1`, and verified `currentSpendPerHr: 0`. Evidence captured in `.docs/research/2026-05-08-runpod-vllm-serverless-provisioning.md`.
- 2026-05-08: Retried the RunPod vLLM data-plane certification on `vllm-zfwlutvio6ilfl` with a single active worker. Health polling for 15 minutes never reached a serving state; the worker cycled between throttled and initializing with no ready/running/unhealthy terminal signal. Because `workers-min=0` update returned ambiguous control-plane state, the endpoint was deleted rather than left as a possible spend source. Final verification: `runpodctl serverless list` -> `[]`, `runpodctl user` -> `currentSpendPerHr: 0`. The reusable template `hkyjvfajm3` remains for a corrected Hub/baked-image follow-up.
- 2026-05-08: Re-aligned the command-line package boundary before adding the installable command surface. Researched Codex/Pi/OpenCode client packaging patterns, recorded the VANTARI decision in `.docs/research/2026-05-08-cli-tui-package-boundary.md`, and started the `apps/cli` package so the backend remains the kernel/protocol owner rather than absorbing every executable surface. The package ships `var.exe` plus PowerShell-safe `vantari.exe` because bare `var` is a PowerShell keyword.
- 2026-05-08: Corrected the installed CLI workspace contract after PowerShell/runtime probes. Bare `vantari` now opens an interactive prompt, bare text is accepted as an initial prompt, kernel child processes inherit the resolved workspace, and workspace resolution defaults to the terminal context (`VANTARI_WORKSPACE` -> explicit `vantari workspace set` override -> current directory ancestors) rather than installer-bound repository state.
- 2026-05-08: Split installed CLI provider auth from workspace/session ownership. `vantari` keeps workspace execution bound to the current terminal directory by default, supports explicit `VANTARI_WORKSPACE`/`vantari workspace set` overrides, and falls back to `%LOCALAPPDATA%\Vantari\auth\auth.json` for provider credentials without writing auth into arbitrary workspaces. The installer now seeds that installed auth ledger when the repo auth ledger exists.
- 2026-05-08: Repaired the line-mode CLI operator feedback gap. Interactive turns now surface durable session failure envelopes instead of silently returning to the prompt, and non-JSON runs emit a `VAR1_STATUS` running envelope to stderr once provider execution starts. Checked the local Zig repo collection for future TUI references; `zml-smi`/`vaxis` is the strongest fullscreen package candidate, while `ziex` is useful only for styled CLI output primitives.
- 2026-05-08: Added a second deep adversarial agent-pipeline matrix with 111 individually named cases. The new suite exercises canonical provider success/failure runs, durable session/output/event evidence, transcript tool-call pairing, unresolved/orphan tool transcript rejection, compacted context suffix reconstruction, poisoned checkpoint tolerance, status failure-clearing semantics, delegated session metadata, and workspace path containment. Validation passed from `apps/backend` with `.\scripts\zigw.ps1 build test --summary all` -> `644/644 tests passed`.
- 2026-05-08: Replaced the incompatible `packages/tui` clone with `rockorager/libvaxis` branch `zig-0.15`, preserving the Vantari package identity as `vantari_tui` and public module name `tui`. Wired `apps/cli` through a local package dependency on `../../packages/tui`, added a CLI import contract test for `@import("tui")`, and validated with `packages/tui` `..\..\apps\backend\scripts\zigw.ps1 build test --summary all` -> `103/104 tests passed; 1 skipped`, `apps/cli` `..\backend\scripts\zigw.ps1 build test --summary all` -> `2/2 tests passed`, and `apps/cli` `..\backend\scripts\zigw.ps1 build --summary all` -> `11/11 steps succeeded`.
- 2026-05-08: Promoted bare installed `vantari` from backend line-mode REPL to the `apps/cli` Vaxis chat shell while preserving explicit command routing through the VAR1 kernel CLI. The shell owns only terminal presentation and calls canonical kernel JSON-RPC methods (`initialize`, `health/get`, `session/create`, `session/send`) through `stdio_rpc.LocalClient`; first-turn prompt ownership was kept on `session/create` to avoid duplicate sends. Patched the Windows Vaxis TTY compatibility edge for Zig 0.15.1 (`std.fs.File.stdin/stdout`, `callconv(.winapi)`), rebuilt, installed to `%LOCALAPPDATA%\Vantari\bin`, and verified with `apps/cli` `..\backend\scripts\zigw.ps1 build --summary all` -> `11/11 steps succeeded`, `apps/cli` tests -> `2/2 passed`, `packages/tui` tests -> `103/104 passed; 1 skipped`, `git diff --check`, installed `vantari.exe --help`, and installed `vantari.exe health --json` -> `ok: true`.
- 2026-05-08: Replaced the first-pass flat TUI renderer with a denser Vaxis operator shell. The viewport now has a status/header band, responsive session rail, central bottom-aligned role-block transcript, runtime/provider rail, and bottom composer while continuing to call only the canonical VAR1 JSON-RPC methods through `stdio_rpc.LocalClient`. Rebuilt and reinstalled `vantari.exe` to `%LOCALAPPDATA%\Vantari\bin`; validation passed with `apps/cli` build -> `11/11 steps succeeded`, `apps/cli` tests -> `2/2 passed`, `packages/tui` tests -> `103/104 passed; 1 skipped`, `git diff --check`, installed `vantari.exe --help`, and installed `vantari.exe health --json` -> `ok: true`.
- 2026-05-08: Switched the active provider in both installed and workspace auth ledgers from `cli-cloud-llm-v1` to existing `zai` credentials (`glm-5.1`, `https://api.z.ai/api/coding/paas/v4`). Verified through installed `vantari.exe health --json` -> `auth_provider: zai`, `model: glm-5.1`, `ok: true`, then proved data-plane execution with installed `vantari.exe run --prompt "Reply with exactly: OK" --json` -> output `OK` for session `session-1778264429662-85af60028f0a4fb4`.
- 2026-05-08: Corrected the actual PowerShell launch-context auth shadow. Bare `vantari` from `C:\Users\Savage` resolved workspace root `C:\Users\Savage` and therefore read `C:\Users\Savage\.var\auth\auth.json` before installed auth; that ledger still had `cli-cloud-llm-v1` active. Copied the active Z.AI ledger into `C:\Users\Savage\.var\auth\auth.json` and verified from `C:\Users\Savage` with `vantari health --json` -> `auth_provider: zai`, `model: glm-5.1`, `workspace_root: C:\Users\Savage`, then `vantari run --prompt "Reply with exactly: OK" --json` -> output `OK` for session `session-1778264632639-7086cd2880ef48e0`.
- 2026-05-08: Refined the installed TUI interaction and visual contract after live use. User prompts now append to local state, clear the composer, and render before the blocking provider RPC starts, so the sent message is visible immediately. Removed the session/runtime side rails, removed the transcript title, removed bootstrap system output, shortened the header to app/status/provider/workspace/plan, and collapsed the palette around the mint accent. Reinstalled `vantari.exe` after stopping locked older processes; validation from `C:\Users\Savage` passed with `vantari health --json` -> `auth_provider: zai`, `model: glm-5.1`, and `vantari run --prompt "Reply with exactly: OK" --json` -> output `OK` for session `session-1778265675785-22b99ee81cfb26db`.
- 2026-05-08: Added real TUI progress visibility for running sessions without claiming provider token streaming. The shell now subscribes to `events/subscribe`, runs blocking `session/send` on a worker thread, drains stdio JSON-RPC `session/event` notifications while the provider/tool loop is active, and renders progress rows for `session_started`, tool request/review/completion, child-agent waiting, compaction, budget, blocked, and failed events before the final assistant response lands. Reinstalled `vantari.exe`; validation passed with `apps/cli` build -> `11/11`, `apps/cli` tests -> `2/2`, `packages/tui` tests -> `103/104 passed; 1 skipped`, `git diff --check`, and from `C:\Users\Savage` `vantari health --json` -> `auth_provider: zai`, plus `vantari run --prompt "Reply with exactly: OK" --json` -> output `OK` for session `session-1778265969833-6cb7e64933338e86`.
- 2026-05-08: Repaired TUI progress visibility after proving the prior notification-only path missed real tool events. A forced Z.AI tool run produced durable events (`tool_requested`, `tool_reviewed`, `tool_completed`, `session_failed`) in `C:\Users\Savage\.var\sessions\session-1778266143677-b15afed33ce7fc58\events.jsonl`, so the TUI now polls canonical `session/get` while a run is active and dedupes progress rows from the durable event ledger, while suppressing the low-value `session_started` row. Added an immediate `thinking: waiting for model` progress row for the first provider wait. Reinstalled `vantari.exe`; validation passed with `apps/cli` build -> `11/11`, `apps/cli` tests -> `2/2`, `packages/tui` tests -> `103/104 passed; 1 skipped`, `git diff --check`, and `vantari health --json` from `C:\Users\Savage` -> Z.AI `glm-5.1` ready.
- 2026-05-08: Added the kernel-owned `shell_exec` command tool with `argv`, `shell`, `bash`, and `powershell` modes, workspace-contained cwd resolution, command-execution review risk, structured stdout/stderr/exit metadata, output budgets, and Windows process timeout termination through Zig stdlib `Child` primitives. Raised the auth-only/default `MAX_STEPS` budget from `1` to `8` so a successful tool call can receive the required post-tool provider response instead of failing with `StepLimitExceeded`. Rebuilt and reinstalled `%LOCALAPPDATA%\Vantari\bin\vantari.exe`; validation passed with backend tests -> `645/645`, CLI build -> `11/11`, CLI tests -> `2/2`, `git diff --check`, installed catalog showing `shell_exec` with `review_risk: command_execution`, and a live Z.AI tool run `session-1778267022369-64d32be9af2050f7` producing `tool_requested -> tool_reviewed -> tool_completed -> assistant_response` plus stdout `VANTARI_SHELL_OK`.
- 2026-05-08: Smoothed the generated-artifact tool flow after live evidence showed a 1000-line HTML request attempted one oversized `write_file`, hit `ToolPayloadExceeded`, then previously fell through the step ceiling. The prompt envelope now contains an explicit large-artifact protocol: small `write_file` seed, numbered `append_file` chunks under 7000 bytes, workspace-relative paths only, and post-write validation. Tool payload hints are specialized for `write_file`, `append_file`, and `replace_in_file`; default tool-call budgets now permit chunked artifact work (`MAX_TOOL_CALLS_PER_TURN=16`, `MAX_TOOL_CALLS_PER_SESSION=96`). Validation passed with backend tests -> `645/645`, CLI build -> `11/11`, CLI tests -> `2/2`, reinstall to `%LOCALAPPDATA%\Vantari\bin`, and live Z.AI flow probe `session-1778267369629-31871c3249ae2dd1` creating `.var/flow-probe/chunked-ledger.txt` via `write_file` + 4 `append_file` calls, validating `220` lines through `shell_exec`, and completing without payload/budget/path errors.
- 2026-05-09: Refined the TUI running-state feed from the `.refs/openai__codex` item lifecycle model: Vantari still reads canonical VAR1 session events, but the terminal now renders compact typed progress rows instead of raw review/runtime payloads. Successful auto-review JSON is suppressed, repeated sibling tool requests collapse (`append_file x3`), completions render as `done: <tool>`, payload failures render as short `error: <tool> (<code>)`, and context/tool/budget/session failures retain terminal visibility. Validation passed with `apps/cli` `..\backend\scripts\zigw.ps1 build test`, `apps/cli` ReleaseSafe build, reinstall via `apps/cli/scripts/install-var.ps1` -> `Build Summary: 11/11 steps succeeded`, and installed `%LOCALAPPDATA%\Vantari\bin\vantari.exe health --json` from `C:\Users\Savage` -> Z.AI `glm-5.1`, `ok: true`.
- 2026-05-09: Promoted provider streaming from presentation wish to kernel contract. `openai_compatible` now supports `stream:true` OpenAI-compatible SSE responses, emits provider text deltas through `StreamHooks`, reconstructs final assistant content/tool calls from SSE chunks, and keeps buffered JSON as the non-stream call path. The executor records each provider text chunk as durable `assistant_delta` session events before tool dispatch and final `assistant_response`; the installed stdio/web host transports now pass `httpSendStreaming`. The TUI consumes `assistant_delta` as live assistant transcript text, removes the synthetic waiting row, and suppresses duplicate final output when deltas already rendered. Validation passed with backend tests, CLI tests, ReleaseSafe CLI build, reinstall to `%LOCALAPPDATA%\Vantari\bin`, a live no-tool Z.AI run `session-1778329584230-d323fb23095a33ab` producing token-level `assistant_delta` events, and a live tool run `session-1778329643658-7167cac42123d18a` producing `assistant_delta -> tool_requested -> tool_reviewed -> tool_completed -> assistant_delta -> assistant_response`.
- 2026-05-09: Added frontier-grade command-output streaming to the canonical VAR1 event ledger. `shell_exec` now forwards bounded stdout/stderr chunks through a typed command-output callback; the Windows process runner drains stdout and stderr pipes on reader threads while the process is still running; the executor records base64-safe `tool_output_delta` events with stream/cap metadata; and the TUI renders those deltas as live progress rows before `tool_completed`. Validation passed with backend tests, CLI build, ReleaseSafe reinstall to `%LOCALAPPDATA%\Vantari\bin`, installed health for `vantari.exe` and `var.exe` through Z.AI `glm-5.1`, and installed live run `session-1778332559852-6c83ec2888265352` proving `assistant_delta -> tool_requested -> tool_reviewed -> stdout tool_output_delta(live-1) -> stdout tool_output_delta(live-2) -> tool_completed -> assistant_delta -> assistant_response`.
- 2026-05-09: Added the root deep-core doctrine requested from the operator log. `AGENTS.md` now persists frontier-reference discipline, no-fallback runtime boundaries, Windows-native proof requirements, deep Zig/C-capability ambition, adversarial pipeline testing, and a 16-item Frontier Roadmap covering typed event grammar, binary-safe event spine, tool spans, interruptible process supervision, C ABI acceleration sockets, allocator quotas, deterministic context compilation, write-intent ledgers, TUI workbench evolution, provider capability probing, delegation supervision, byte-level session integrity, and performance telemetry. The executor now emits typed `tool_started` and `tool_finished` spans with duration/error metadata before the legacy `tool_completed` breadcrumb; the TUI renders those spans as compact `start:`/`done:`/`error:` rows. Validation passed with backend tests, CLI tests, ReleaseSafe reinstall, installed health, and live installed run `session-1778333036992-dbbfe2cd1eadc395` proving `tool_started -> tool_finished -> tool_completed` plus streamed assistant deltas while reading root `AGENTS.md`.
- 2026-05-09: Corrected installed-client workspace resolution after root `AGENTS.md` proof exposed an override-order defect. `VANTARI_WORKSPACE` remains explicit authority, but current-directory project markers and canonical session roots now win before the installed workspace override, so `vantari` launched from `VANTARI-ONE` binds to the repo root while `apps/backend` still binds to its own runtime lane. Rebuilt `apps/cli` in `ReleaseSafe`, reinstalled `%LOCALAPPDATA%\Vantari\bin\vantari.exe` and `var.exe`, validated installed health from repo root and `apps/backend`, and ran installed Z.AI smoke `session-1778337298992-740b4860a1e8bf90` proving `tool_started -> stdout tool_output_delta(VANTARI_STREAM_PROOF_A) -> stdout tool_output_delta(VANTARI_STREAM_PROOF_B) -> tool_finished -> assistant_delta -> assistant_response`.
- 2026-05-09: Hardened the new streaming/tool/TUI surfaces with adversarial capability-pressure tests instead of diagnostics-only checks. Added executor coverage for streamed assistant deltas before and after a tool call with strict phase ordering, tool-runtime coverage proving `shell_exec` forwards stdout, stderr, and cap markers through the typed event sink, TUI formatter coverage for bounded/malformed/binary output chunks, and provider coverage for CRLF-delimited SSE streams with sparse multi-tool indexes. The pressure pass surfaced two real contract gaps: `CommandLimits` was not exported through the public tool-runtime namespace, and final SSE reconstruction used LF-only event splitting while live emission already understood CRLF. Both were corrected. Validation passed with `apps/backend` `zig build test --summary all` -> `647/647 tests passed`, `apps/cli` -> `2/2 tests passed`, and `packages/tui` -> `103/104 tests passed; 1 skipped`.
- 2026-05-09: Mined `.refs/openai__codex` and `.refs/badlogic__pi-mono` for test-pressure patterns and translated the useful ones into VANTARI-native coverage without copying their architecture. Codex contributed command lifecycle/streaming-output constraints and turn/item event pressure; Pi Mono contributed provider-matrix edges (`empty`, tool-call-without-id/result, stream cases) plus terminal overlay pressure for bounded/mixed progress rows. Added tests for empty SSE objects, streamed tool calls without IDs, shell command shape rejection before process launch, cwd escape rejection before process launch, command limit clamping, mixed TUI tool rows, and long progress-row truncation. The first run intentionally failed on cwd escape classification, proving the boundary is `PathOutsideWorkspace` rather than generic `InvalidArguments`; the test was split to preserve that sharper invariant. Validation passed with `apps/backend` `650/650 tests passed`, `apps/cli` `2/2 tests passed`, and `packages/tui` `103/104 tests passed; 1 skipped`.
- 2026-05-09: Added another user-facing TUI pressure slice focused on what the operator sees while using Vantari, not diagnostics. `apps/cli` now runs `src/tui_chat.zig` as an explicit test target, raising the CLI gate to 9 executed tests. New coverage asserts assistant token deltas coalesce into a single visible response block, repeated command output rows remain visible instead of deduping away useful progress, repeated non-output progress rows dedupe, assistant text after tool progress starts a fresh readable block, transcript/header labels stay compact, mixed tool rows remain readable, long progress rows stay bounded, and ANSI color/control sequences are stripped from streamed command output. The newly active test target surfaced a blank-row bug for malformed base64 output chunks; the formatter now renders `<invalid output chunk>` instead. Validation passed with `apps/cli` `9/9 tests passed`, `apps/backend` `650/650 tests passed`, and `packages/tui` `103/104 tests passed; 1 skipped`.
- 2026-05-09: Added a second operator-facing TUI pressure slice for real terminal behavior. Streamed command output now treats carriage-return progress updates as overwrites, handles backspace edits, and strips ANSI CSI color controls before rendering progress rows, so spinners/progress bars/colorized tools do not pollute the transcript. Tool request summaries now group repeated mixed batches by tool type (`read_file x2, shell_exec x2`) instead of only collapsing all-identical lists. Added executable tests for carriage-return overwrite rendering, backspace rendering, grouped mixed tool summaries, and whitespace-tolerant tool list parsing. Validation passed with `apps/cli` `11/11 tests passed`, `apps/backend` `650/650 tests passed`, and `packages/tui` `103/104 tests passed; 1 skipped`.
- 2026-05-09: Harvested the local Codex/Pi Mono references again before the next TUI/backend stream slice. Adopted Codex's item-lifecycle/delta pressure and Pi Mono's runtime-owned streaming-state constraint as smaller VANTARI primitives: the TUI durable event cursor now uses ordered ledger position plus event-key replay suppression instead of timestamp-only skipping, preserving same-millisecond assistant/tool/output bursts without duplicating notification/poll events; PageUp/PageDown/Ctrl+Home/Ctrl+End transcript scrollback now lets operators inspect prior chat and tool progress without mutating composer state; and the prompt envelope now requires concise operator-visible progress before tool batches while explicitly forbidding hidden chain-of-thought exposure. The native installer now deploys ReleaseSafe Windows binaries. Validation passed with `apps/cli` `13/13 tests passed`, `apps/backend` `650/650 tests passed`, `packages/tui` `103/104 tests passed; 1 skipped`, `git diff --check`, installed `%LOCALAPPDATA%\Vantari\bin\vantari.exe health --json` from `C:\Users\Savage` -> Z.AI `glm-5.1`, and live event-ledger proof `session-1778343391413-e19459801608c49e` showing `assistant_delta -> tool_requested -> tool_started -> tool_output_delta -> tool_finished -> assistant_delta -> assistant_response`.
- 2026-05-09: Corrected the real fullscreen TUI streaming blocker after live use showed deltas accumulated and then appeared all at once. Root cause: `stdio_rpc.LocalClient.call` holds its stdin mutex until the response returns, so the TUI's worker-thread `session/send` was still blocking same-client `session/get` polling behind the long provider request. The TUI now sends turns through a dedicated worker `LocalClient` bound to the same workspace while the visible client stays free to poll the append-only session event ledger every 60ms. Reinstalled ReleaseSafe Windows binaries after stopping locked installed `vantari.exe` processes. Validation passed with `apps/cli` `13/13 tests passed`, `apps/backend` `650/650 tests passed`, installed `%LOCALAPPDATA%\Vantari\bin\vantari.exe health --json` -> Z.AI `glm-5.1`, and dual-client live proof `session-1778343754719-599eb903bcfa280a` where observer `session/get` saw events before `session/send` completed and the final ledger contained streamed `assistant_delta` plus eight `tool_output_delta` rows for `VANTARI_LIVE_1..4`.
- 2026-05-09: Tightened the first-response TUI affordance so the operator sees immediate assistant activity before provider first-token latency. On submit the TUI now inserts a replaceable `Starting...` assistant block, draws immediately, and then replaces it with the first real `assistant_delta`; if tool progress arrives before assistant text, the placeholder is removed and progress becomes the live signal. Failure/no-output paths remove the placeholder instead of leaving synthetic transcript residue. Added executable TUI tests for placeholder replacement and tool-progress-first removal. Validation passed with `apps/cli` `15/15 tests passed`, `apps/backend` `650/650 tests passed`, ReleaseSafe reinstall to `%LOCALAPPDATA%\Vantari\bin`, installed health from `C:\Users\Savage` -> Z.AI `glm-5.1`, and `git diff --check`.
- 2026-05-09: Simplified the Vantari TUI chrome around the actual operator scan contract. The header now occupies two rows and renders only `Vantari`, the workspace basename, and the active provider; status, plan/subscription copy, and extra labels were removed from the persistent top band. The footer now occupies three rows with a quiet `Message` label and direct editor surface; the `>` prompt marker and keyboard-instruction chatter were removed. Validation passed with `apps/cli` `15/15 tests passed`, `apps/backend` `650/650 tests passed`, `packages/tui` `103/104 tests passed; 1 skipped`, `git diff --check`, ReleaseSafe reinstall to `%LOCALAPPDATA%\Vantari\bin`, and installed `vantari.exe health --json` from `C:\Users\Savage` -> Z.AI `glm-5.1`, `ok: true`.
- 2026-05-09: Refined the installed TUI waiting state and tool rows after operator feedback. The header now shows the full workspace path, the footer removes the `Message` label entirely and vertically centers the editor row, tool lifecycle events update one keyed progress row (`running` -> duration/error) instead of appending request/start/done rows, and the pre-token assistant placeholder is stored as an empty pending assistant block rendered as blinking mint `thinking` rather than synthetic transcript text. Validation passed with `apps/cli` `16/16 tests passed`, `apps/backend` `650/650 tests passed`, `packages/tui` `103/104 tests passed; 1 skipped`, `git diff --check`, ReleaseSafe reinstall to `%LOCALAPPDATA%\Vantari\bin`, and installed `vantari.exe health --json` from `C:\Users\Savage` -> Z.AI `glm-5.1`, `ok: true`.
- 2026-05-09: Added first-class skill awareness to the VAR1/Ventari prompt and tool contract. The prompt envelope now includes a compact Skill Routing Contract with native capsules for `planning-spec`, `insect`, `dupe-audit`, `recon-intel`, `ux-playbook`, `t3-tape`, `repo-harvester`, `playwright`, and `task-audit`; the built-in `skill_info` read-only tool exposes exact native/add-on skill capsules without injecting every global `SKILL.md` into context. The capsule set was checked against `%USERPROFILE%\.codex\skills` and `%USERPROFILE%\.claude\skills`. Validation passed with `apps/backend` `651/651 tests passed`, `apps/cli` `16/16 tests passed`, `packages/tui` `103/104 tests passed; 1 skipped`, ReleaseSafe reinstall to `%LOCALAPPDATA%\Vantari\bin`, installed `vantari.exe health --json` -> Z.AI ready, and installed `vantari.exe tools --json` showing `skill_info` available.
- 2026-05-09: Hardened the installed TUI presentation and long-run routing contract after live operator feedback. Foreground text styles now carry explicit surface/panel backgrounds to remove terminal-default gray highlight artifacts, mouse wheel events drive transcript scrollback, `MAX_STEPS` defaults to `32`, the prompt/catalog now tells agents that `wait_agent` accepts explicit `timeout_ms`, and the Insect native skill capsule names `%USERPROFILE%\.codex\skills\insect-rs-runtime` plus `scripts\run-insect-rs.ps1` directly. Validation passed with `apps/backend` `651/651 tests passed`, `apps/cli` `16/16 tests passed`, `packages/tui` `103/104 tests passed; 1 skipped`, `git diff --check` whitespace-clean with existing LF/CRLF warnings only, ReleaseSafe reinstall to `%LOCALAPPDATA%\Vantari\bin`, installed `vantari health --json` from `C:\Users\Savage` -> Z.AI `glm-5.1`, and installed `vantari tools --json` showing `wait_agent` `timeout_ms` plus `skill_info`.
- 2026-05-10: Added another user-facing pressure-test slice focused on weak visible workflow seams rather than diagnostics. The active CLI TUI gate now asserts scroll anchoring while live command output continues below the viewed transcript and verifies assistant deltas after tool progress open a separate readable response block instead of merging across progress rows. The active backend provider gate now asserts streamed tool-capable requests opt into `parallel_tool_calls` and reconstructs split SSE `data:` fields so first visible tokens are not dropped when a provider frames a chunk across continuation lines. Added `apps/backend/tests/provider_test.zig` to the aggregate harness and exposed a narrow provider testing surface for request/response contract probes. Validation passed with `apps/backend` `653/653 tests passed` and `apps/cli` `18/18 tests passed`.
- 2026-05-10: Simplified the fullscreen TUI chrome into a clean unframed chat surface. Removed the persistent top header band, removed the header/composer separator borders, moved the composer up above a subdued bottom metadata row, and rendered only the necessary workspace/model/provider context in faded text at the bottom. Added an executable layout test to preserve the no-top-chrome contract and keep the metadata below the composer. Validation passed with `apps/cli` `19/19 tests passed`, `apps/backend` `653/653 tests passed`, `packages/tui` `103/104 tests passed; 1 skipped`, `git diff --check` whitespace-clean with existing LF/CRLF warnings only, ReleaseSafe build, reinstall to `%LOCALAPPDATA%\Vantari\bin`, and installed `vantari health --json` from `C:\Users\Savage` -> Z.AI `glm-5.1`, `ok: true`.
- 2026-05-10: Smoothed the active TUI streaming loop without adding presentation weight. The running turn now keeps consuming queued terminal input, mouse scroll, resize, and cancel keys while `session/send` executes on the worker client; durable `session/get` replay is throttled to a 350ms fallback interval instead of firing every frame because notifications remain the primary stream; assistant token append now grows the existing buffer instead of reallocating through full concatenation per delta; and command stdout/stderr deltas coalesce into one bounded per-tool stream row so long output stays live without transcript row spam. Added executable pressure coverage for bounded streamed output rows and updated stream-row tests to preserve latest evidence. Validation passed with `apps/cli` `20/20 tests passed`, ReleaseSafe build, reinstall to `%LOCALAPPDATA%\Vantari\bin`, `git diff --check` whitespace-clean with existing LF/CRLF warnings only, and installed `vantari health --json` from `C:\Users\Savage` -> Z.AI `glm-5.1`, `ok: true`.
- 2026-05-10: Refined the TUI tool-output projection after comparing the local Codex app-server item lifecycle contract. Vantari still keeps canonical stdout/stderr chunks in the append-only event spine, but the terminal read model now renders command/code output as a single-line bounded preview: terminal controls are normalized, multiline payloads flatten into scan-friendly text with a line-count suffix, repeated stream deltas append with ` | ` inside one keyed row, and long stream rows keep the latest evidence behind a `stdout: ...` prefix. Added executable tests for flattened hostile chunks, code-shaped multiline output, and bounded one-line stream rows. Validation passed with `apps/cli` `21/21 tests passed`, ReleaseSafe build, reinstall to `%LOCALAPPDATA%\Vantari\bin`, `git diff --check` whitespace-clean with existing LF/CRLF warnings only, and installed `vantari health --json` from `C:\Users\Savage` -> Z.AI `glm-5.1`, `ok: true`.
- 2026-05-10: Continued the TUI premium-pass on the operator-facing chat surface. Runtime/system rows now render as dense single-line transcript items with no blank-row tax, the bottom command deck formats workspace/model/provider inside the terminal width while preserving full workspace text when room exists, and only actionable transient state (`Esc cancel`, `cancelling`, scrollback depth) appears in the faded footer. Added executable pressure tests for compact runtime rows, footer truncation, and footer transient-state priority. Validation passed with `apps/cli` `zig build test`, ReleaseSafe reinstall to `%LOCALAPPDATA%\Vantari\bin`, installed `vantari.exe health --json`, and installed `vantari.exe tools --json`.
- 2026-05-11: Closed the installed-workspace/auth fallback test hole that let a fresh child directory under `C:\Users\Savage` inherit an ancestor `.var` runtime root and fail provider execution with `BadStatus`. CLI workspace resolution now treats ancestor `.var/auth` and `.var/sessions` as ownership only at the invocation root or behind a real project marker, while explicit `VANTARI_WORKSPACE`, repo markers, canonical sessions, and installed workspace fallback keep their defined order. Added aggregate backend tests for ancestor-auth rejection, ancestor-session rejection, current-directory auth, project-marker precedence, installed override, and installed-auth fallback/local-auth precedence; the gate now runs `661/661` tests instead of the previous false-green `653/653`. Validation passed with backend tests, CLI tests, `git diff --check`, ReleaseSafe reinstall to `%LOCALAPPDATA%\Vantari\bin`, installed repo-root Z.AI smoke `REPO_OK`, and installed fresh-child smoke from `C:\Users\Savage\VantariProbe-20260511-014914` resolving that directory and returning `CHILD_OK` through global auth.
- 2026-05-11: Closed the TUI transcript projection test hole that let long assistant messages appear to lose content during scrollback, send, or final response replacement, then corrected the follow-on render ownership fault that caused a ReleaseSafe installed TUI segmentation fault after submit. The fullscreen TUI now projects transcript messages into explicit terminal rows before viewport selection, scrolls by visual row instead of message index, preserves scroll anchors as streamed assistant deltas wrap, trims trailing spacer rows from the bottom viewport, and preserves blank/indented assistant lines. Transcript rows now borrow durable `ChatState` message slices because Vaxis cells retain grapheme slices until `vx.render`; temporary owned row strings are forbidden by regression coverage. Added adversarial CLI coverage for wrapped-row exposure, viewport sweep reachability, final thinking-placeholder replacement, streamed-delta row anchor growth, blank/indented line preservation, durable render-slice ownership, streamed-realloc buffer ownership, progress-output coalescing ownership, actual screen-cell retained-slice ownership, and compact runtime rows. Validation passed with `apps/cli` `33/33 tests passed`, `apps/backend` `661/661 tests passed`, `git diff --check` whitespace-clean with existing LF/CRLF warnings only, ReleaseSafe reinstall to `%LOCALAPPDATA%\Vantari\bin`, and installed health from `E:\Workspaces\01_Projects\01_Github\MEMBRANE-FRAMEWORK` resolving Z.AI `glm-5.1`.
- 2026-05-11: Closed the shell_exec tool-error observability gap exposed by live MEMBRANE use. Schema/tool-contract failures now persist actionable repair hints directly on the typed `var1.tool_finished.v1` event, `shell_exec` advertises the argv-vs-command exclusivity and Windows PowerShell-native query contract in its usage hint, and the TUI keeps hinted failures in the same keyed tool row instead of requiring the suppressed legacy completion breadcrumb. Added adversarial backend and CLI tests for typed repair hints plus single-row TUI projection. Validation passed with `apps/backend` `661/661 tests passed` and `apps/cli` `34/34 tests passed`.
- 2026-05-11: Added the follow-on adversarial test mesh for the MEMBRANE shell_exec failure class. Runtime-loop coverage now simulates a provider emitting malformed `shell_exec` arguments (`mode:"shell"` plus `argv`), proves the kernel returns provider-visible repair evidence with schema, usage hint, and Windows PowerShell-native guidance, and proves the typed event ledger persists the same hint before the legacy completion breadcrumb. Tool catalog coverage now pins the `shell_exec` argv-vs-command contract, Windows `Select-String/Get-ChildItem` guidance, and self-repairing `renderExecutionError` payload before process launch. Prompt coverage asserts the same command-shape guidance is present in the model-visible envelope. Validation passed with `apps/backend` `664/664 tests passed`.
- 2026-05-11: Expanded the command/tool/TUI blanket tests around the new implementation. Backend tool tests now prove `shell_exec` dispatches `argv`, `powershell`, and platform `shell` modes through distinct command shapes without mixing argument contracts, and they assert specialized `renderExecutionError` hints for timeout, payload cap, missing executable, oversized writes, and workspace escape classes. CLI TUI tests now prove stdout/stderr deltas for one `shell_exec` call stay in separate keyed rows, repeated stdout deltas coalesce with cap evidence, and hostile long hinted tool errors replace the running row while staying bounded. Validation passed with `apps/backend` `666/666 tests passed` and `apps/cli` `36/36 tests passed`.
