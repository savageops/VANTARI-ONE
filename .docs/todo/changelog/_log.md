- 2026-04-19: Initialized the agent-harness research blueprint artifact set, normalized the future runtime root to `.harness/`, and harvested primary sources plus supplemental Insect artifacts.
- 2026-04-19: Added a dedicated Mermaid architecture document derived from the blueprint so the harness layer model, turn flow, stage pipeline, and state machine have a cold-start visual view.
- 2026-04-19: Bootstrapped `variant-1/` as the Zig implementation root with a variant-local toolchain wrapper, env files, runtime/docs ownership roots, source/test/script skeletons, and a baseline README.
- 2026-04-19: Filled the `variant-1` core runtime with env loading, `.harness` task persistence, `.docs` sync enforcement, an OpenAI-compatible provider adapter, a minimal CLI loop, and a passing 9-test suite.
- 2026-04-19: Final validation for `variant-1` passed the automated Zig suite, but live proof from WSL hit `ConnectionRefused` to `127.0.0.1:1234`; Windows-host diagnostics confirmed the endpoint is up there and that the configured `gemma-4-e2b-it` model currently answers the strawberry sanity check incorrectly.
- 2026-04-19: Cross-compiled `variant-1` to `zig-out/bin/VAR1.exe`, ran it from Windows inside the `variant-1` workspace, and confirmed the harness reaches the configured endpoint there; the executable returns the model's current answer `There are **two** "r"s in the word "strawberry".`
- 2026-04-19: Re-ran the native `variant-1` Zig suite after the Windows portability fixes; `./scripts/zigw.sh build test --summary all` remains green at `9/9 tests passed`.
- 2026-04-19: Added `variant-1/architecture.md` as the canonical Mermaid architecture map for the current Zig runtime, including the real module graph, success and failure sequences, disk ownership, task state machine, and the planned simple file-tool insertion boundary.
- 2026-04-19: Upgraded `variant-1` into a tool-capable coding harness with built-in file/search tools, provider-visible tool descriptors, a bounded multi-step tool loop, updated README/architecture/env contracts, a cold-start structural snapshot, and green validation at `15/15 tests passed` plus a successful Windows cross-compile and `tools` CLI check on both native and Windows executables.
- 2026-04-20: Switched `variant-1/.env` to `mudler/gemma-4-26b-a4b-it-apex-gguf/gemma-4-26b-a4b-apex-i-balanced.gguf` for live validation; the Windows `proof` run now returns 3 r's correctly, but the staged hero-page exercise still fails on repeated `PathOutsideWorkspace` file-tool errors and ends in `StepLimitExceeded`, confirming the blocker is the Windows workspace-root guard rather than the model choice.
- 2026-04-20: Fixed `variant-1` Windows workspace-root resolution by canonicalizing `HARNESS_WORKSPACE` once at startup and resolving relative tool paths against that frozen absolute root; validation now passes at `17/17 tests passed`, Windows cross-compile succeeds, and live hero-page task `task-1776637631052` completes the staged `write_file -> read_file -> write_file` flow to create and refine `variant-1/playground/hero-page-exercise/index.html`.
- 2026-04-20: Replaced `variant-1` `list_files` discovery with a native workspace walker so Windows live runs no longer depend on external `rg`; native validation is now green at `18/18 tests passed` and the Windows cross-compile still succeeds.
- 2026-04-20: Resumed the blocked refinement validation after the `list_files` unblock; Windows live task `task-1776638586949` completed `list_files -> read_file -> write_file -> read_file -> write_file` and upgraded `variant-1/playground/hero-page-exercise/index.html` into a premium SaaS landing page.
- 2026-04-20: Ran a deeper section-aware HTML navigation exercise against `variant-1`; the agent followed segmented `read_file` ranges, safely expanded `index.html` with tabs/data/testimonials/FAQ, and exposed that the original `HARNESS_MAX_STEPS=6` budget was one step too low for the richer preserved-edit workflow.
- 2026-04-20: Raised `variant-1` live bounded headroom from `HARNESS_MAX_STEPS=6` to `HARNESS_MAX_STEPS=10` in `.env` and `.env.example`, then reran the same deeper HTML workflow to successful completion on Windows as task `task-1776640038095`.
- 2026-04-20: Switched `variant-1` to the user-supplied z.ai `GLM-5.1` provider with the prior LM Studio config preserved as commented backup; added env-parser coverage for commented backup lines and corrected provider URL handling so explicit versioned bases like `/v4` do not receive an extra `/v1`.
- 2026-04-20: Hardened `variant-1` HTTPS provider transport for large coding-model responses by routing HTTPS calls through bounded `curl`, raising the subprocess max-time for long-form generation, and increasing response capture to 4 MiB; live proof succeeded and task `task-1776644675444` created and refined `variant-1/playground/hero-page-exercise/index-v2.html`.
- 2026-04-20: Replaced `variant-1` mixed provider transport with one native in-process HTTP/TLS path in `src/provider.zig`, removed subprocess/`curl`/temp-payload behavior entirely, and added a loopback transport regression plus task-id uniqueness coverage; native validation is now green at `22/22 tests passed`.
- 2026-04-20: Proved the no-fallback transport live against the active z.ai `GLM-5.1` config: `proof` returned 3 r's for strawberry and a second run used `read_file` to inspect `variant-1/playground/hero-page-exercise/index-v2.html` and return the document title without damaging the artifact.
- 2026-04-20: Locked the `variant-1` delegation architecture to one canonical execution primitive: run/task/thread. Updated the `013` planning chain, `variant-1/README.md`, `variant-1/architecture.md`, and a new cold-start snapshot to state that subagents are normal child runs linked by `parent_task_id`, while the current `.harness/agents/` layer is transitional drift to remove during implementation.
- 2026-04-20: Implemented task-linked delegation in `variant-1` by extending canonical task metadata with `parent_task_id`/`display_name`/`agent_profile`, resuming child runs through `--task-id`, and collapsing `launch_agent`/`agent_status`/`wait_agent`/`list_agents` onto `.harness/tasks/<task-id>/` instead of a second durable agent registry.
- 2026-04-20: Fixed the resumed-child prompt path and the provider non-200 double-free in `variant-1`, raised delegation coverage to `26/26 tests passed`, and proved live parent-to-child execution with parent task `task-1776686988276-8a2cca76dc158194`, child task `task-1776686999873-d9c4db1cc9366323`, and final answer `3`.
- 2026-04-20: Validated a real 3-child `variant-1` design competition with parent task `task-1776694198947-7bf3868b743294f7`; the parent completed launch/list/status/wait/report supervision cleanly over canonical child task state, while children produced three distinct HTML artifacts and exposed the current bounded ceilings (`exit code 6` on two children, `StepLimitExceeded` on one).
- 2026-04-20: Switched the live `variant-1` validation path to the local LM Studio/OpenAI-compatible provider `qwen3.6-35b-a3b-apex`, proved the Windows executable local proof path again, and then ran a new 3-child compact-round-variation competition where the parent supervision completed successfully but all three children failed `BadStatus` before reaching the 1000-line floor, exposing a weak local-model throughput/turn-budget profile under concurrent child orchestration.
- 2026-04-20: Refined `variant-1` child supervision so canonical task snapshots now expose journal-backed progress metadata, `wait_agent` returns a current snapshot on timeout instead of failing, and the repo docs/Mermaid now distinguish run-level resumability from full chat-session UI behavior.
- 2026-04-21: Added `variant-1` CLI `--prompt-file` support, tightened the canonical proof prompt to an explicit character-sequence contract, and added CLI prompt-resolution coverage so Windows-host delegated validation no longer depends on shell quoting.
- 2026-04-21: Switched the live local validation target to `gemma-4-26b-a4b-it-apex`; `./scripts/zigw.sh build test --summary all` passed at `30/30 tests passed`, the Windows cross-build passed, and the bounded `./scripts/local_gemma_smoke.sh` completed successfully with direct proof task `task-1776768760477-6b0cfb248ea7c6c4` plus delegated parent/child tasks `task-1776768761140-58866867d2348ad` / `task-1776768761930-f39ea625d180e1e2`, each returning `3`.
- 2026-04-21: Closed the `018` consumer-workbench slice on native Windows by adding `variant-1` PowerShell operator scripts, revalidating the suite at `34/34 tests passed`, passing the full `local_gemma_smoke.ps1` workbench flow with task `task-1776774550694-45ac4946056a93bd`, and aligning README, architecture, Mermaid, snapshot, and todo-chain evidence to the shipped `serve` plus embedded-UI projection over canonical task state.
- 2026-04-21: Hardened the shipped `variant-1` workbench into a cleaner local single-user chat shell, moved smoke scratch and workbench logs under `.zig-cache/smoke/`, embedded a favicon to eliminate the browser 404, and revalidated with `34/34 tests passed`, a passing `local_gemma_smoke.ps1` run on task `task-1776778021956-42e781c4c8b4efb8`, and a Playwright snapshot plus screenshot with no console errors.
- 2026-04-21: Hardened `variant-1` tool descriptors and retry payloads so file and agent tools now publish example JSON, usage hints, and echoed `parameters_schema` on failures; the `rg_search (FileNotFound)` loop now returns actionable recovery guidance and the Zig suite revalidated at `43/43 tests passed`.
- 2026-04-22: Ran a repo-wide living-doc sync after tool-descriptor hardening: corrected the remaining stale `41/41` current-state note, clarified that the shared `.harness/tasks/<task-id>/` contract currently resolves to `variant-1/.harness/tasks/<task-id>/` for the shipped runtime because `HARNESS_WORKSPACE=.`, linked the descriptor-hardening snapshot from the harness docs index, and documented the shared `parameters_schema` plus `contract_example` plus `usage_hint` repair contract across root-harness docs.
- 2026-04-22: Hardened child-orchestration UX in `variant-1` by adding lifecycle heartbeat fields (`LIFECYCLE_STATE`, `HEARTBEAT_*`, `NEXT_PARENT_ACTION`) to child snapshots, enforcing parent-side follow-through while children are still non-terminal, masking internal child-tool labels in run logs, and adding response guardrails so operator-facing replies avoid leaking internal supervision commands; validation re-ran green at `45/45 tests passed`.
- 2026-04-22: Hardened `variant-1` operator discoverability by moving command help into canonical `src/cli.zig` help text, adding machine-readable `tools --json` schema export from `src/tools.zig`, suppressing Zig stack traces for invalid CLI argument sets, syncing the README/help contract, and revalidating at `49/49 tests passed` plus live Windows `--help`, `run --help`, `proof --help`, `help tools`, and `tools --json` checks.
- 2026-04-22: Added canonical continued-run support to `variant-1` by extending task records with `continued_from_task_id`, replaying prior prompt-plus-final-answer turns in `src/loop.zig`, teaching the embedded workbench composer to continue a selected or pasted completed task id, syncing README and architecture docs to the new boundary, and revalidating at `52/52 tests passed`.
- 2026-04-22: Fixed a Variant-1 response-sanitization regression where leaked internal tool names were being replaced with the child-supervision wait sentence even when no child runs existed; `src/loop.zig` now redacts tool names to operator-safe phrasing instead of emitting false wait-state copy, and `tests/runtime_loop_test.zig` now locks this contract plus a tool-documentation allow-path. Validation is currently blocked by unrelated pre-existing compile drift in `src/web.zig` (`renderTaskTurnsResponse` and `ContinuationValidationError` undeclared), with an additional strict cleanup applied in `src/store.zig` (`var` -> `const`) to clear one blocker.
- 2026-04-22: Replaced Variant-1 operator continuation semantics with same-task session resume by adding canonical `turns.jsonl` transcripts under each task, routing follow-up messages through `POST /api/tasks/:id/messages`, teaching `src/loop.zig` to read and update the transcript on the same task id, collapsing workbench nesting back to real child runs only, syncing README plus architecture plus a new snapshot to the corrected contract, and revalidating at `56/56 tests passed`.
- 2026-04-22: Synced Variant-1 docs to current validation reality: revalidated `build test` at `56/56`, refreshed CLI/help/schema/proof notes, corrected `.docs` ownership language to keep `.harness` as the only runtime source of truth, and marked the current `local_gemma_smoke.ps1` gate as failing at `55/56` with `tests/web_test.zig:117` (`error.MissingField`) while preserving the last known full smoke and Playwright success date (`2026-04-21`).
- 2026-04-22: Renamed the live runtime identifier from `variant_1` to `VAR1` across the Zig package/module/executable/help/script/doc surfaces while preserving the checked-in `variant-1/` folder path as the stable checkout location, added the `vantari.one` naming note in the main README, regenerated the Zig package fingerprint for the renamed package identity, normalized remaining historical `variant_1` references outside the append-only user-message log, and revalidated with `.\scripts\zigw.ps1 build test --summary all` (`56/56 tests passed`), `.\scripts\proof.ps1` (`3`), and `.\zig-out\bin\VAR1.exe --help`.
- 2026-04-22: Removed the hardwired `proof` product surface from `VAR1`, replaced it with a generic `health` readiness command plus `health.ps1` and `health.sh`, switched the live smoke path to direct `run --prompt` plus `/api/health`, deleted stale parallel Node/temp root artifacts from `variant-1/`, normalized the remaining live `.harness` helper to `VAR1.exe`, cleaned the active ignore list of obsolete `proof-*` scratch, and revalidated with `56/56 tests passed`, `VAR1.exe health --json`, a passing `local_gemma_smoke.ps1`, a zero-pair scoped dupe audit, and a passing Playwright browser sanity check.
- 2026-04-22: Ran a second product-hardening pass on `VAR1` to remove remaining demo-oriented live UI surfaces: dropped the embedded quick-prompt launcher from `src/ui/index.html`, renamed the browser title from `VAR1 Local Workbench` to `VAR1 Workbench`, tightened operator-facing shell copy around canonical task sessions, taught `tests/web_test.zig` to assert that the old launcher strings are absent, ignored and cleaned generated `.playwright-cli/` scratch, and revalidated with `56/56 tests passed`, `scripts/health.ps1`, a green `local_gemma_smoke.ps1`, zero-pair scoped dupe audit output, and a fresh Playwright browser snapshot showing the cleaned workbench surface without the removed prompt-launch buttons.
- 2026-04-24: Pulled `origin/main` into the root checkout with a fast-forward after stashing local state, restored the local `README.md` and harness directories, and merged the overlapping root `.gitignore` plus `apps/backend/variant-1/.env.example` content so upstream tracked files and local operator-safe rules both remain intact.
- 2026-04-22: Ran an Insect-led transport-boundary research pass for the harness kernel, stored raw official specs plus YouTube/deep-web transcripts under `.docs/research/2026-04-22-harness-transport-architecture/`, and recorded the architectural decision to use a headless `JSON-RPC 2.0` kernel over local `stdio` with LSP-style `Content-Length` framing while treating HTTP/WebSocket/UI surfaces as outer adapters rather than core harness ownership.
- 2026-04-22: Replaced the older mixed target/current harness research blueprint with a strict headless-kernel architecture set: rewrote `.docs/research/agent-harness-architecture-blueprint.md` around `JSON-RPC 2.0` over `stdio` with `Content-Length` framing, rewrote the Mermaid companion to show only the target kernel-plus-clients split, added `.docs/research/agent-harness-megalithic-playbook.md` as the ordered migration contract from the current `VAR1` monolith, and linked the new set from `.harness/docs/_index.md` plus the transport research packet.
- 2026-04-22: Normalized the `VANTARI-ONE` monorepo root without moving the live app: added a root README, corrected canonical root and app docs to point at `apps/backend/variant-1`, added root-normalization research plus a cold-start structural snapshot, cleaned `zigw` wrapper cache/toolchain labels away from `agent-harness-experimental`, and revalidated `VAR1` with `.\scripts\zigw.ps1 build test --summary all` (`56/56 tests passed`) plus `.\scripts\health.ps1` (`status: ready`).
- 2026-04-22: Hardened `apps/backend/variant-1/scripts/zigw.ps1` so the renamed `VANTARI-ONE` Windows wrapper path self-heals truncated archives, can reuse the last known-good legacy install/archive during the repo-root rename, and resumes interrupted extraction safely; current validation is green at `.\scripts\zigw.ps1 version` -> `0.15.1`, `.\scripts\zigw.ps1 build test --summary all` -> `56/56 tests passed`, and `.\scripts\health.ps1` -> `status: ready`.
- 2026-04-22: Relaxed `apps/backend/variant-1/scripts/local_gemma_smoke.ps1` to gate on functional `3` reporting instead of exact cosmetic equality for delegated output, added CLI coverage plus shipped help text for `GET /api/tasks/:id/turns` and `POST /api/tasks/:id/messages`, and revalidated with `.\scripts\zigw.ps1 build test --summary all` -> `57/57 tests passed`, `.\scripts\local_gemma_smoke.ps1` -> pass, `.\scripts\health.ps1` -> `status: ready`, and `.\zig-out\bin\VAR1.exe help serve` showing the new routes.
- 2026-04-22: Closed the post-review doc drift on the same readiness slice by syncing the remaining `apps/backend/variant-1` README plus architecture validation counts to `57/57 tests passed` and revalidating the publish-facing lane with a fresh green `.\scripts\zigw.ps1 build test --summary all`, `.\scripts\health.ps1`, `.\scripts\local_gemma_smoke.ps1`, and `.\zig-out\bin\VAR1.exe help serve`.
- 2026-04-22: Remediated the architect-review findings across the live `VAR1` slice by replacing the shipped `rg_search` catalog contract with canonical `search_files` on top of `iex` while keeping a compatibility alias internally, hardening `web.zig` to log and persist detached background failures instead of swallowing them, narrowing smoke cleanup from kill-all `VAR1` behavior to explicit port/PID ownership, teaching the bash wrappers to hand off cleanly to native Windows paths in Git Bash, and making root/worktree docs explicit that the imported `VANTARI-ONE` root is still documented-but-disabled for worktree control until it is reattached to a real Git checkout; revalidated with `.\scripts\zigw.ps1 build test --summary all` -> `58/58 tests passed`, `.\scripts\zigw.ps1 build --summary all`, `.\zig-out\bin\VAR1.exe tools --json` showing `search_files`, `.\scripts\health.ps1` -> `status: ready`, `.\scripts\local_gemma_smoke.ps1` -> pass, `bash -lc "./scripts/local_gemma_smoke.sh"` -> pass, and `git rev-parse --show-toplevel` still failing as the expected documented root-state proof.
- 2026-04-23: Replaced the root README with an iEx-style public contract rewritten for VANTARI-ONE's current monorepo truth, added a root `.gitignore` plus `apps/backend/variant-1/.env.example` so local secrets and Zig/runtime artifacts stay out of a future public commit, and recorded a public-readiness review showing the remaining blockers: only `README.md` and `LICENSE` are committed today, the backend lane currently fails `.\scripts\zigw.ps1 build test --summary all` because `build.zig` is missing, and the documented Zig source owners are not present in the published tree.
- 2026-04-23: Deepened the root README again to match the stronger `savageops/iEx` style more closely: added a full badge rail including release/download metadata, expanded the title and section hierarchy, embedded repo-native Mermaid architecture diagrams from the harness blueprint set, linked the key root/backend docs directly, and kept the release gate explicit so the richer README does not overstate current repo readiness.
- 2026-04-24: Cloned `badlogic/pi-mono` into `.refs/badlogic__pi-mono` as a local study corpus, added `.gitignore` rules so `.refs` upstream clones stay local while `.refs/sources` remains available, and recorded the specific repo-level patterns worth borrowing for VANTARI-ONE in `.docs/research/2026-04-24-pi-mono-reference-notes.md`: strong root README framing, explicit AGENTS plus CONTRIBUTING governance, clean package ownership, and concrete root validation commands.
- 2026-04-24: Extended the `pi-mono` study into direct Zig-harness guidance in `.docs/research/2026-04-24-pi-mono-lessons-for-zig-harness.md`, mapping the upstream provider/agent/coding-shell split onto VANTARI's headless-kernel direction, documenting what to copy versus what not to copy, and framing the next correct move as internal Zig boundary extraction (`core/`, `host/`, `clients/`) rather than adding more workbench surface.
- 2026-04-24: Added `.docs/research/2026-04-24-auth-persistence-study.md` after comparing the live Zig lane against the local `pi-mono` reference: VANTARI should persist provider auth in one canonical `.var/auth/auth.json` ledger, keep subscription-plan metadata on the same provider record, resolve credentials through one refresh-aware auth resolver, and keep the browser client non-authoritative for secrets.
- 2026-04-24: Removed the live `.harness` surface in favor of canonical `.var`: migrated the local root and app state trees from `.harness/` to `.var/`, renamed legacy `tasks/` directories to `sessions/` plus `task.json`/`task.md` to `session.json`/`session.md`, synced the current root/app docs and tests to `.var/sessions`, replaced the remaining Zig 0.15-incompatible `std.json.stringifyAlloc` call sites in the protocol-backed CLI/kernel path with formatter-backed helpers, and fixed `LocalClient.deinit` on Windows so `health` exits cleanly after protocol teardown. Validation: `.\scripts\zigw.ps1 build --summary all` succeeded, `.\scripts\zigw.ps1 build test --summary all` passed `58/58`, `.\scripts\health.ps1` returned `status: ready`, and both root and app `.harness` directories no longer exist.
- 2026-04-24: Completed the session-native headless-kernel cut for `apps/backend/variant-1` by finishing the `.var/sessions/<id>/session.json + messages.jsonl + events.jsonl + output.txt` contract, rewriting stale session/web/runtime tests to the new JSON-RPC bridge shape, removing the embedded `src/ui/index.html` surface in favor of the external `apps/frontend/var1-client` client, fixing a real Windows stdio-client crash caused by spawning the reader thread against a stack-local `LocalClient`, and hardening the smoke scripts to use bridge wording plus authenticated provider/model preflight checks. Validation: `./scripts/zigw.ps1 build test --summary all` -> `61/61 tests passed`; `./scripts/health.ps1` -> `status: ready`; `./scripts/local_gemma_smoke.ps1` is currently blocked by local provider state and now reports the real cause explicitly: `OPENAI_MODEL=gemma-4-26b-a4b-it-apex` is not currently served by LM Studio at `http://127.0.0.1:1234/v1/models` (available model observed: `text-embedding-nomic-embed-text-v1.5`).
- 2026-04-24: Saved planning-spec chain `021-codex-subscription-auth` under `.docs/todo/pending/` for later implementation of Codex subscription auth: `.var/auth/auth.json` remains the canonical auth ledger, API-key parity is preserved, OAuth login/device-code work is isolated from transport, Codex subscription requests get a dedicated provider boundary, and terminal verification requires Zig tests plus sanitized auth status evidence before archival.
- 2026-04-24: Locked the next VAR1 context-builder baseline without changing runtime code: logged the user direction, refreshed local Pi/Codex refs, added root `AGENTS.md` with project-local `.var/sessions` and context ownership rules, documented `.docs/research/2026-04-24-context-builder-baseline.md`, and corrected `.var/README.md` so the local runtime root no longer describes `.harness`.
- 2026-04-24: Landed the first VAR1 context-builder implementation slice: `messages.jsonl` now gets stable `msg-N` IDs plus monotonic `seq` values with tolerant legacy reads, final assistant output appends instead of rewriting transcript history, `context.jsonl` has append/read-latest checkpoint helpers, `src/core/context/builder.zig` owns session-storage-to-provider-message assembly, `loop.zig` delegates transcript construction to that builder, bridge/test mocks were updated for the expanded message contract, docs now include `context.jsonl` in the canonical `.var/sessions` layout, validation assets under `.docs`, `apps/backend/variant-1/.docs`, `scripts`, `tests`, and the app README are no longer ignored by the root `.gitignore`, and `local_gemma_smoke.ps1` now fails fast when `/v1/models` hangs instead of stalling silently. Validation: `.\scripts\zigw.ps1 build test --summary all` -> `65/65 tests passed`; `.\scripts\health.ps1` -> `status: ready`; `.\scripts\local_gemma_smoke.ps1` currently blocks on provider readiness with `http://127.0.0.1:1234/v1/models` timing out.
- 2026-04-24: Completed the follow-up retrace and pluggable hierarchy lock: verified the context-builder seam against local Pi/Codex references, added `src/core/tools/` as a tiny tool socket facade over the existing runtime, added `src/core/plugins/` as manifest/socket validation only, documented the no-empty-loader wall-socket doctrine in `AGENTS.md`, captured the reference-backed hierarchy baseline in `.docs/research/2026-04-24-pluggable-backend-hierarchy-baseline.md`, and clarified `architecture.md` so future plugin implementations stay outside `core/`. Validation is intentionally limited to the Zig lane for this code slice.
- 2026-04-24: Closed the retrace validation gap by adding integration tests through the public `VAR1.core.tools` and `VAR1.core.plugins` namespaces, then revalidated the slice with `.\scripts\zigw.ps1 build test --summary all` (`67/67 tests passed`), `git diff --check` (clean except existing CRLF conversion warnings), and `.\scripts\health.ps1` (`status: ready`).
- 2026-04-28: Completed the pre-commit documentation sweep for the `VAR1` public checkpoint: corrected the app docs/runtime-plane READMEs from stale `.harness` task/journal wording to `.var/sessions` session/message/context/event/output ownership, refreshed current validation notes in the root README, app README, and architecture map, re-ran `.\scripts\zigw.ps1 build test --summary all` (`67/67 tests passed`) plus `.\scripts\health.ps1` (`status: ready`), and prepared the repo for a single publishable checkpoint commit.
- 2026-04-28: Refreshed the root `README.md` after a live `savageops/iEx` README review: added a public-facing badge/nav rail, a repo-native SVG architecture banner, quick-start/operator/session sections, stronger Mermaid architecture flow, and an explicit CI/public-readiness caveat so the landing page borrows the iEx presentation strength without overstating VANTARI-ONE runtime readiness.
- 2026-04-28: Recut the root `README.md` from a verbose repo-status landing page into a higher-signal harness architecture document: removed reference-credit wording, dropped mundane topology inventory, centered the copy on kernel ownership, session ledger semantics, protocol boundaries, context compilation, and capability surfaces, while preserving the current validation gate and public CI caveat.
- 2026-04-28: Rewrote the root `README.md` against the senior-engineering documentation contract: one-sentence purpose statement, objective architecture overview, concrete capability and protocol matrices, session-ledger/dataflow diagrams, exact configuration parameters from the live backend lane, and no comparative marketing or implementation-origin narration.
- 2026-04-28: Renamed the public VAR1 configuration contract from provider/framework-prefixed keys to canonical `BASE_URL`, `API_KEY`, `MODEL`, `WORKSPACE`, and `MAX_STEPS` across the parser, templates, smoke scripts, tests, README surfaces, and health protocol output.
- 2026-04-28: Collapsed the flat backend source layout into canonical ownership folders: `clients/`, `host/`, `shared/`, and `core/{agents,auth,config,context,docs,executor,plugins,providers,sessions,tools}`; updated live docs and pending auth plans to the new module paths.
- 2026-04-28: Removed `harness` wording from the live VAR1 naming surface: tool contracts now use workspace/session vocabulary, public README links no longer point at old harness-blueprint files, app docs say `VAR1 Zig Kernel`, startup/events say `VAR1 session initialized`, and `.harness` remains only in the explicit legacy migration path and its tests.
- 2026-04-28: Removed the remaining legacy compatibility surface from VAR1: deleted `.harness` and `.var/tasks` store migration, removed task-named store aliases and task-field parsing, dropped the `/api/tasks*` HTTP facade, removed the old `agent-harness-experimental` Zig cache fallback, updated generated session ids to `session-...`, and aligned docs/tests with the new-only `.var/sessions` plus `/rpc`/`/events`/`/api/health` contract.
- 2026-04-28: Completed a second structural parity pass across the VAR1 backend: reduced the root export surface to `shared`, `core`, `host`, and `clients`; made `messages.jsonl` the only full transcript reader for both UI hydration and context compilation; kept `context.jsonl` as the compact checkpoint ledger consumed only by `core/context/builder.zig`; removed the stale `rg_search` alias and the leftover `canonicalToolName` path; normalized workspace-state contracts from task wording to `session`, `request`, and `todo_name`; and refreshed public validation counts to the current `62/62` gate. Validation: `.\scripts\zigw.ps1 build test --summary all` -> `62/62 tests passed`; `.\scripts\health.ps1` -> `status: ready`.
- 2026-04-28: Added `.docs/handoff/2026-04-28-var1-new-chat-dump.md` as the cold-start transfer artifact for the current VAR1 architecture slice, capturing the canonical `.var/sessions` ledger, namespace ownership, context-builder contract, validated drift scans, dirty-tree warning, and the next clean `session/compact` implementation path.
- 2026-04-28: Implemented the first manual `session/compact` slice for VAR1: added `core/context/compactor.zig` as the Zig-native checkpoint writer, exposed the protocol capability through `session/compact`, preserved `messages.jsonl` as the complete transcript, appended compact summaries to `context.jsonl`, proved builder consumption from `first_kept_seq`, documented the cold-start structure in `.docs/handoff/2026-04-28-var1-session-compact-snapshot.md`, narrowed the JSON wire value `keep_recent_messages` to `u32` before widening internally so Windows-gnu executable builds do not instantiate host-word unsigned parsing, and revalidated with `.\scripts\zigw.ps1 build test --summary all` -> `65/65 tests passed`, `.\scripts\health.ps1` -> `status: ready`, and `git diff --check` clean except existing CRLF conversion warnings.
- 2026-04-28: Upgraded VAR1 `session/compact` from whole-range manual summaries to an entry-aware checkpoint primitive: `max_entries_per_checkpoint` can advance one `messages.jsonl` row or a bounded segment, checkpoints now record `aggressiveness_milli` plus `compacted_entry_count`, higher public `aggressiveness` recompacts the covered range from the immutable transcript, global and repo AGENTS doctrine now captures future-first simplicity, and validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `67/67 tests passed`.
- 2026-04-28: Audited the VAR1 tool initialization path after the `rg` to `iex` search replacement: confirmed `search_files` is the content-search tool backed by the external `iex` executable, `list_files` remains native workspace discovery, provider tool schemas are emitted from `ToolDefinition` through `builtinDefinitionsForContext`, plugin/tool sockets currently validate contracts without runtime loading, and the next clean architecture slice is a canonical per-tool module registry plus explicit dependency availability metadata for command-backed tools.
- 2026-04-28: Updated VAR1 tool/runtime documentation across root and variant-1 architecture docs, `.docs` research snapshots, `.var` docs, and the handoff dump. The docs now make the static `ToolDefinition` catalog, `search_files` external `iex` boundary, native `list_files` discovery, validation-only plugin sockets, and next per-tool registry/availability slice explicit.
- 2026-04-28: Researched high-signal README structures from Kubernetes, VS Code, Rust, and Moby, then upgraded the tracked VANTARI-ONE README surfaces around concise identity, reader routing, quick-start commands, runtime boundaries, and public-only follow-up links. Validation: README inventory found root/backend/frontend plus one ignored playground README; `git diff --check` passed with only existing CRLF conversion warnings; iEx stale-link scan found no `.docs` public links or old tagline fragments.
- 2026-04-28: Corrected the root README product framing after review: removed the misleading `replay, inspection, and execution boundary` pitch language, reframed VANTARI-ONE as a local agent harness for running, extending, and supervising AI agent sessions, and kept replay-like behavior only as an internal consequence of session history rather than a public product claim. Validation: iEx scan found no remaining `replay`, `inspection`, `execution boundary`, or `runtime owner` wording in tracked README surfaces; `git diff --check` passed with only existing CRLF conversion warnings.
- 2026-04-28: Expanded the product-framing correction across the active documentation set after another README research pass over LangChain, Electron, Ollama, Kubernetes, VS Code, Rust, and Moby. The tracked docs now frame VANTARI-ONE as an agent harness first, with user/operator capabilities before architecture; `AGENTS.md`, root README, backend README, backend architecture, frontend README, current handoff, tool-boundary note, and the ignored SVG banner were normalized away from replay/inspection/execution-boundary wording. Historical append-only logs, raw source captures, and old completed planning records were left intact as provenance.
- 2026-04-28: Ran the post-Convex-restart orchestrator smoke pass against live VAR1 evidence: revalidated the Zig lane at `67/67 tests passed`, confirmed `health` readiness on `gemma-4-26b-a4b-it-apex`, checked the static `tools --json` catalog, and distilled the next high-value backlog around tool modularity, browser session-operator parity, bridge security hardening, auth completion, and stale-doc parity repair.
- 2026-04-29: Implemented VAR1 tool capability truthfulness and local bridge hardening: split the built-in file/agent tools into canonical `core/tools/builtin/*.zig` modules where each tool owns its definition, availability, and execute contract; added a per-tool availability registry with `search_files` gated on real `iex` command resolution; exposed availability metadata in `VAR1 tools --json`; rejected unavailable command-backed search before execution; replaced wildcard bridge CORS with explicit local-origin allowlisting; added a bridge-token health handshake for `/rpc` and `/events`; redacted sensitive bridge-visible health fields; added audit logging for session/auth/write-capable RPC actions; updated the static browser client to send the bridge token; and revalidated with `.\scripts\zigw.ps1 build test --summary all` -> `72/72 tests passed` plus `.\zig-out\bin\VAR1.exe tools --json` showing `search_files` `external_command` `iex` available.
- 2026-04-29: Completed the documentation parity pass for the tool-capability and bridge-hardening slice: updated the root README, backend README, backend architecture map, browser-client README, and tool-boundary research note so the documented runtime now matches the per-tool module registry, availability metadata catalog, `search_files -> iex` dependency gate, local-origin bridge policy, bridge-token handshake, redaction, audit behavior, and current `72/72` Zig validation gate.
- 2026-04-29: Implemented the VAR1 context-window blueprint: added optional `.var/config/settings.toml` context policy for auto/manual compaction, context window size, threshold ratio, reserve tokens, recent-message retention, checkpoint chunking, aggressiveness, and provider-overflow retry; added budget and overflow classifier primitives; wired executor auto-threshold compaction and one-shot provider-overflow retry through the existing `core/context/compactor.zig`; kept `messages.jsonl` append-only and `context.jsonl` as the checkpoint ledger; updated README and architecture docs to the verified contract; and revalidated with `.\scripts\zigw.ps1 build test --summary all` -> `78/78 tests passed`.
- 2026-04-29: Closed the bridge-origin hardening follow-up by removing `Origin: null` from the allowed browser policy, documenting the static client as a local HTTP surface instead of a direct-file page, and adding route coverage proving null-origin denial plus explicit localhost success.
- 2026-04-29: Completed a cleanup/refactor pass over the live VAR1 architecture slice: extracted bridge origin/token/redaction/audit helpers into `src/host/bridge_access.zig`, kept `src/host/http_bridge.zig` focused on route transport, made tool availability registry lookup table-driven from module-owned tool names/specs, added integration coverage for both seams, refreshed README and architecture documentation, confirmed `search_files` reports real `iex` availability through `VAR1 tools --json`, and revalidated with `.\scripts\zigw.ps1 build test --summary all` -> `80/80 tests passed`.
- 2026-04-30: Closed the accepted VAR1 quality-review findings by extending bridge redaction from sensitive keys to secret-shaped string values, deriving agent-tool availability through the agent module instead of hand-indexed registry entries, rejecting unknown `[context]` settings keys, and adding non-destructive stale `VAR1.exe` diagnostics to `scripts/health.ps1`; docs now record the fail-closed config and current validation, and validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `83/83 tests passed`, `.\scripts\health.ps1` -> `status: ready`, and `.\zig-out\bin\VAR1.exe tools --json` showing `search_files` `iex` availability.
- 2026-04-30: Closed the durable bridge-audit follow-up by replacing process-stderr-only bridge audit logging with append-only redacted `var1.bridge_audit.v1` JSONL records at `.var/audit/bridge.jsonl`, wiring audited session/auth/write-capable `/rpc` requests to persist audit records before kernel dispatch, sharing the bridge redactor across health/error/RPC/event payloads, adding route and sink redaction tests, and refreshing root/backend architecture docs to the persisted audit contract. Validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `86/86 tests passed`.
- 2026-04-30: Revalidated VAR1 against the intentionally smaller local model `gemma-4-e2b-it`: switched ignored local `.env` plus active `.var/auth/auth.json` provider state to the target model, made `scripts/local_gemma_smoke.ps1` model-parametric with effective-health verification, `-AllowSanityMismatch` small-model probe mode, exact `curl.exe` token-header emission for canonical `/rpc` bridge checks, and explicit model-capability verdicts, documented the `.env` seed versus `.var/auth` effective-source boundary, and proved the harness surfaces with `health` ready on `gemma-4-e2b-it`, narrow `read_file` and `search_files` tool calls working through event traces, a token-authenticated `/rpc` bridge session completing, and manual `session/compact` writing a checkpoint. The strict strawberry sentinel consistently returned `2` instead of `3`, so the tolerant smoke now completes while reporting `sanity_answer_verified=false`, identifying model reasoning degradation rather than a harness transport failure. Validation passed with `.\scripts\local_gemma_smoke.ps1 -ExpectedModel gemma-4-e2b-it -AllowSanityMismatch -Port 4311` -> `GEMMA_LOCAL bridge ok` and embedded `86/86 tests passed`, plus prior `.\scripts\health.ps1` -> `status: ready`.
- 2026-04-30: Ran a deeper `gemma-4-e2b-it` complex-tool smoke against ignored `.zig-cache/smoke` artifacts: the model successfully invoked `list_files`, `search_files`, `read_file`, `write_file`, `append_file`, and `replace_in_file` event paths, then a repair pass successfully invoked read/write/append/replace and produced an executable `.ps1` missing-config branch; however, the first pass falsely reported success after writing Python into a `.ps1` file and losing the report structure, the repair pass left a bash shebang before `param(...)` and appended without a newline, and the child-agent supervision pass completed `launch_agent`/`agent_status`/`wait_agent` while returning a contradictory inspection result. Conclusion: the harness surfaces tool execution and durable evidence correctly, but smaller-model autonomy requires deterministic artifact validators after mutating tool calls.
- 2026-04-30: Added lightweight VAR1 mutating-tool effect receipts after comparing the live small-model smoke gap against local Pi/Codex-style event/evidence boundaries and current OpenAI guardrail/tracing patterns. `write_file`, `append_file`, and `replace_in_file` now preserve the existing `ok/tool/content` response shape and add `var1.tool_effect.v1` metadata with requested/resolved path, before/after byte counts, operation-specific counts, and SHA-256 evidence; README and architecture notes now describe the shipped receipt contract. Validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `86/86 tests passed`, and `.\zig-out\bin\VAR1.exe tools --json` still reports `search_files` `iex` availability.
- 2026-04-30: Tightened the same effect-receipt slice after a second architecture pass: effect action and metric names are now typed in `core/tools/module.zig` instead of free-form strings, write/replace receipts derive post-state hashes from committed buffers, append receipts derive deterministic expected post-state from the pre-read plus appended payload, and validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `86/86 tests passed`, `.\scripts\health.ps1` -> `status: ready` on `gemma-4-e2b-it`, and `.\zig-out\bin\VAR1.exe tools --json` preserving `search_files` `iex` availability.
- 2026-04-30: Made VAR1 mutating-tool receipts effect-first for model-visible content while preserving the structured `effect` JSON object and legacy `PATH`/`BYTES` compatibility. `write_file`, `append_file`, and `replace_in_file` now begin tool content with `EFFECT_SCHEMA var1.tool_effect.v1` and `EFFECT_KEY effect`, giving smaller local models semantic file-effect evidence before byte accounting. Validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `86/86 tests passed`, `.\scripts\health.ps1` -> `status: ready`, a sequential raw-prefix harness run returning `EFFECT_SCHEMA`/`EFFECT_KEY effect`, and a key-probe harness run returning `HAS_LITERAL_EFFECT_KEY: yes`.
- 2026-04-30: Stored the user-provided Z.AI coding provider in the ignored `.var/auth/auth.json` ledger as active provider `zai` without recording the secret in docs, normalized the ledger to UTF-8 without BOM after Windows PowerShell encoding broke kernel RPC framing, and compared upgraded `develop` (`56d3eb8`) against clean rebuilt prior commit `3d33a01` on the same `glm-5.1` endpoint. Both lanes passed health and provider sentinel probes; the upgraded lane reported `FIRST_CONTENT_LINE=EFFECT_SCHEMA var1.tool_effect.v1` for a mutating `write_file` result, while the prior lane reported legacy `FIRST_CONTENT_LINE=PATH ...`, so the effect-first receipt upgrade is worth keeping for model-visible tool-result grounding.
- 2026-04-30: Extended the Z.AI `glm-5.1` comparison from receipt presence into an adherence benchmark against upgraded `develop` (`56d3eb8`) and prior commit `3d33a01` with isolated Zig cache roots to prevent cross-worktree binary contamination. Both lanes obeyed the provider sentinel and completed the same mutating-tool flows, but the upgraded lane gave the model explicit effect evidence (`EFFECT_SCHEMA`, action, byte counts, after-hash) that improved evidence-grounded answers: it extracted schema ownership and a 64-character post-write hash when those fields existed, and its replace/write answers could cite visible after-state fields instead of deriving them from context. The prior lane stayed functional but relied on legacy `PATH`/count text and produced at least one correct-looking answer from information absent in the tool result. Conclusion: the upgrade improves harness-mediated instruction adherence for tool-result-dependent tasks, but it is not a full model-obedience fix because `glm-5.1` still showed formatting drift and occasional extra tool selection.
- 2026-04-30: Added a canonical VAR1 prompt subsystem under `src/core/prompts/` so prompt assembly no longer lives inside the tool runtime. The new envelope injects hidden kernel guardrails, optional workspace-relative system/developer prompt files from `[prompts]` settings, and the live tool-use contract/catalog as one OpenAI-compatible system-role message; prompt settings fail closed on unknown keys and absolute paths. Tool descriptors were hardened with clearer call-order, schema-repair, exact-edit, child-supervision, and workspace-state hints. Documentation and the progress log now describe the prompt-layer contract. Validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `88/88 tests passed`.
- 2026-04-30: Compared prompt-layer `develop` (`834a632`) against `main` (`3d33a01`) using isolated Zig cache roots and the same Z.AI `glm-5.1` auth state. Both binaries built and reported healthy provider state; the upgraded lane exposed the new descriptor catalog, completed a six-operation file-tool benchmark in session `session-1777576359915-3cf77bc839898869` (`write_file -> read_file -> append_file -> read_file -> replace_in_file -> read_file`) and returned exactly `BENCH_OK beta=TWO gamma=three`. The main lane started session `session-1777576409385-a2b609f0db4508dc`, completed only `write_file`, and failed with `StepLimitExceeded`, leaving the artifact unappended and unreplaced. Verdict: the prompt-envelope upgrade is worth keeping for model tool-plan adherence under tight step budgets.
- 2026-05-01: Closed the `023` agent-loop contract hardening chain from the five accepted review findings. Tool argument parsing now rejects undeclared JSON keys before side effects, tool-call assistant turns and tool results are durable `messages.jsonl` rows, tool execution has independent per-turn and per-session budgets with `tool_budget_exceeded` failure events, the HTTP bridge dispatches accepted connections to detached workers, and prompt settings parse quoted TOML string scalars instead of blind comment stripping. Root/backend docs, `.env.example`, and `.docs/progress_log.md` were refreshed to the shipped contract. Validation passed with `.\scripts\zigw.ps1 build test --summary all` -> `90/90 tests passed`.
- 2026-05-04: Researched Flue (`withastro/flue`) from the provided `flueframework.com` link, cloned upstream into `.refs/withastro__flue`, harvested transferable session/compaction/delegation/connector primitives into `.docs/research/2026-05-04-flueframework-harvest.md`, and recorded a VAR1-specific adopt/adapt/reject implementation matrix with ordered next-slice recommendations.
- 2026-05-04: Refreshed public-facing validation language before publishing the hardening/research checkpoint. Current local gates passed with `.\scripts\zigw.ps1 build test --summary all` -> `90/90 tests passed`, `.\scripts\health.ps1` -> `status: ready`, `.\zig-out\bin\VAR1.exe health --json` -> `ok: true`, and `.\zig-out\bin\VAR1.exe tools --json` -> strict tool schemas plus `iex` availability metadata.
